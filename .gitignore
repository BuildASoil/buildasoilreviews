# Dependencies
node_modules/

# Secrets - NEVER commit these
.env
.env.local
.env.production

# Next.js build
.next/
out/

# Debug logs from sync
data/debug-bad-reviews.json

# OS
.DS_Store
Thumbs.db

# NOTE: We DO commit data/reviews.db so Vercel can read it at runtime.
# Refresh reviews by running `npm run sync` locally, then commit + push.
